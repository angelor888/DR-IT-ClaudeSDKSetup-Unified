const axios = require('axios');

// Configuration
const SLACK_BOT_TOKEN = process.env.SLACK_BOT_TOKEN;
const CHANNEL_ID = 'C094JMFJEDD'; // #it-report

async function sendSessionReport() {
  const message = {
    channel: CHANNEL_ID,
    blocks: [
      {
        type: "header",
        text: {
          type: "plain_text",
          text: "🎯 DuetRight Dashboard V3 - Session Report",
          emoji: true
        }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*Session Date:* July 14, 2025\n*Focus:* Jobber Integration Error Resolution & Dashboard V3 Setup"
        }
      },
      {
        type: "divider"
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*🔧 Jobber 500 Error Fix*\n\n• *Issue:* Jobs API returning 500 errors due to incorrect endpoint\n• *Root Cause:* Using `/customers` instead of `/clients` endpoint\n• *Solution:* Updated JobberService to use correct endpoint pattern\n• *Status:* ✅ Resolved - Jobs now loading successfully"
        }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*📝 Key Changes Made*\n\n• Updated `JobberService.getJobs()` to use `/clients/${clientId}/jobs`\n• Fixed endpoint URL construction in the service\n• Verified integration with live Jobber API\n• Confirmed data flow through dashboard"
        }
      },
      {
        type: "divider"
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*📊 Dashboard V3 Current Status*\n\n• *Backend API:* ✅ Running on port 3005\n• *Frontend:* ✅ Deployed to Firebase\n• *Database:* ✅ Firestore configured\n• *Integrations:* ✅ Jobber, QuickBooks, SendGrid active"
        }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*🚀 Working Features*\n\n• Customer Management (CRUD operations)\n• Job Management with Jobber sync\n• Communications hub (Email/SMS)\n• Real-time sync with external services\n• Authentication & session management"
        }
      },
      {
        type: "divider"
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*⚠️ Outstanding Issues*\n\n• Frontend API calls need backend URL update (currently using localhost)\n• Some TypeScript build warnings in dashboard\n• Slack integration webhook endpoints need testing\n• Production deployment configuration pending"
        }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*🔄 Next Steps*\n\n1. Update frontend to use production backend URL\n2. Complete Slack webhook integration testing\n3. Set up production environment variables\n4. Deploy backend to cloud hosting\n5. Configure custom domain and SSL"
        }
      },
      {
        type: "context",
        elements: [
          {
            type: "mrkdwn",
            text: "🤖 Generated by Claude Code | Dashboard V3 Session Report"
          }
        ]
      }
    ]
  };

  try {
    console.log('📤 Sending session report to #it-report...');
    
    const response = await axios.post(
      'https://slack.com/api/chat.postMessage',
      message,
      {
        headers: {
          'Authorization': `Bearer ${SLACK_BOT_TOKEN}`,
          'Content-Type': 'application/json'
        }
      }
    );

    if (response.data.ok) {
      console.log('✅ Report sent successfully!');
      console.log(`📱 Message posted to #it-report`);
      console.log(`🔗 Message timestamp: ${response.data.ts}`);
    } else {
      console.error('❌ Failed to send report:', response.data.error);
    }
  } catch (error) {
    console.error('❌ Error sending report:', error.message);
    if (error.response) {
      console.error('Response data:', error.response.data);
    }
  }
}

// Run the report
sendSessionReport();