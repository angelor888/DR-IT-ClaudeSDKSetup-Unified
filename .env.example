# Slack Configuration
# Create a Slack app at https://api.slack.com/apps
# Add bot token scopes: chat:write, channels:read, channels:history, users:read, team:read
# Install to workspace and copy the bot token (starts with xoxb-)
export SLACK_TOKEN="xoxb-your-token-here"

# Machine identification (set this on each machine)
# Options: megan, morgan
export MACHINE_NAME="megan"